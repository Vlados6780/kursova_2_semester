<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Comments for <span th:text="${targetUser.username}"></span></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<h1>Comments for <span th:text="${targetUser.username}"></span></h1>
<a th:href="@{/feedback}">Back to Feedback</a>
<h2>User Details</h2>
<p>Username: <span th:text="${targetUser.username}"></span></p>
<p>Age: <span th:text="${targetUser.age}"></span></p>
<p>Email: <span th:text="${targetUser.email}"></span></p>

<h2>Comments</h2>
<div th:if="${comments.isEmpty()}">No comments yet.</div>
<ul>
    <li th:each="comment : ${comments}">
        <strong th:text="${comment.authorUsername}"></strong>:
        <span th:text="${comment.content}"></span>
        (<span th:text="${comment.postedAt}"></span>)
    </li>
</ul>

<h2>Add Comment</h2>
<div th:if="${#fields.hasErrors('newComment')}" class="error">
    <span th:errors="*{newComment.content}"></span>
</div>
<form th:action="@{/feedback/{id}/comment(id=${targetUser.idUser})}" th:object="${newComment}" method="post">
    <label for="content">Comment:</label>
    <textarea id="content" th:field="*{content}" required></textarea>
    <button type="submit">Submit</button>
</form>
</body>
</html>