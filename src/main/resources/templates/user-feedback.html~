<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="UTF-8">
  <title>User Feedback</title>
</head>
<body>
<h2>Feedback for <span th:text="${targetUser.username}"></span></h2>
<p>Email: <span th:text="${targetUser.email}"></span></p>
<p>Age: <span th:text="${targetUser.age}"></span></p>

<h3>Comments</h3>
<ul>
  <li th:each="comment : ${comments}">
    <p><span th:text="${comment.content}"></span></p>
    <p>By: <span th:text="${userDao.findById(comment.authorId).get().username}"></span> on <span th:text="${#dates.format(comment.postedAt, 'yyyy-MM-dd HH:mm')}"></span></p>
  </li>
</ul>

<h3>Add Comment</h3>
<form th:action="@{/feedback/{id}(id=${targetUser.idUser})}" method="post" th:object="${newComment}">
  <div><label>Comment: <textarea th:field="*{content}" required></textarea></label></div>
  <button type="submit">Submit</button>
</form>

<form th:action="@{/logout}" method="post">
  <button type="submit">Logout</button>
</form>
</body>
</html>